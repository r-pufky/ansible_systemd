---
# Always load everything before removing to prevent dependency failures.
#
# TODO(role): Add remaining units as implmentation requires: socket device swap
#     target path slice scope.
- name: 'Load mount'
  ansible.builtin.include_tasks: 'load/mount.yml'
  loop: '{{ systemd_mounts | default([]) }}'
  loop_control:
    loop_var: 'systemd'

- name: 'Load automount'
  ansible.builtin.include_tasks: 'load/automount.yml'
  loop: '{{ systemd_automounts | default([]) }}'
  loop_control:
    loop_var: 'systemd'

- name: 'Load service'
  ansible.builtin.include_tasks: 'load/service.yml'
  loop: '{{ systemd_services | default([]) }}'
  loop_control:
    loop_var: 'systemd'

- name: 'Load timer'
  ansible.builtin.include_tasks: 'load/timer.yml'
  loop: '{{ systemd_timers | default([]) }}'
  loop_control:
    loop_var: 'systemd'

- name: 'Enable mount'
  ansible.builtin.include_tasks: 'enable/mount.yml'
  loop: '{{ systemd_mounts | default([]) }}'
  loop_control:
    loop_var: 'systemd'

- name: 'Enable automount'
  ansible.builtin.include_tasks: 'enable/automount.yml'
  loop: '{{ systemd_automounts | default([]) }}'
  loop_control:
    loop_var: 'systemd'

- name: 'Enable service'
  ansible.builtin.include_tasks: 'enable/service.yml'
  loop: '{{ systemd_services | default([]) }}'
  loop_control:
    loop_var: 'systemd'

- name: 'Enable timer'
  ansible.builtin.include_tasks: 'enable/timer.yml'
  loop: '{{ systemd_timers | default([]) }}'
  loop_control:
    loop_var: 'systemd'

- name: 'Disable service'
  ansible.builtin.include_tasks: 'disable/service.yml'
  loop: '{{ systemd_services | default([]) }}'
  loop_control:
    loop_var: 'systemd'

- name: 'Disable timer'
  ansible.builtin.include_tasks: 'disable/timer.yml'
  loop: '{{ systemd_timers | default([]) }}'
  loop_control:
    loop_var: 'systemd'

- name: 'Disable automount'
  ansible.builtin.include_tasks: 'disable/automount.yml'
  loop: '{{ systemd_automounts | default([]) }}'
  loop_control:
    loop_var: 'systemd'

- name: 'Disable mount'
  ansible.builtin.include_tasks: 'disable/mount.yml'
  loop: '{{ systemd_mounts | default([]) }}'
  loop_control:
    loop_var: 'systemd'

- name: 'Unload service'
  ansible.builtin.include_tasks: 'unload/service.yml'
  loop: '{{ systemd_services | default([]) }}'
  loop_control:
    loop_var: 'systemd'

- name: 'Unload timer'
  ansible.builtin.include_tasks: 'unload/timer.yml'
  loop: '{{ systemd_timers | default([]) }}'
  loop_control:
    loop_var: 'systemd'

- name: 'Unload automount'
  ansible.builtin.include_tasks: 'unload/automount.yml'
  loop: '{{ systemd_automounts | default([]) }}'
  loop_control:
    loop_var: 'systemd'

- name: 'Unload mount'
  ansible.builtin.include_tasks: 'unload/mount.yml'
  loop: '{{ systemd_mounts | default([]) }}'
  loop_control:
    loop_var: 'systemd'
