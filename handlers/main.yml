---
###############################################################################
# Systemd Handlers
###############################################################################

- name: 'Handlers | reload systemd'
  when: (systemd_daemon_reload_enable | default(true))
  ansible.builtin.systemd_service:
    daemon_reload: true
