---
###############################################################################
# Default
###############################################################################
# Role applied with no settings should not change anything.
#
# Tests:
# * Use directory stat to ensure no changes are applied to systemd.

dependency:
  name: 'galaxy'
driver:
  name: 'podman'
provisioner:
  name: 'ansible'
  config_options:
    defaults:
      interpreter_python: 'auto_silent'
      callback_whitelist: 'profile_tasks, timer, yaml'
      fact_caching: 'jsonfile'  # Systemd default services snapshot.
      fact_caching_connection: '/tmp/facts_cache'
      fact_caching_timeout: 7200
    ssh_connection:
      pipelining: false
platforms:
  - name: 'systemd-debian-13-default'
    image: 'ghcr.io/hifis-net/debian-systemd:13'
    systemd: 'always'
    volumes:
      - '/sys/fs/cgroup:/sys/fs/cgroup:ro'
    command: '/lib/systemd/systemd'
    pre_build_image: true
verifier:
  name: 'ansible'
lint: |
  set -e
  yamllint .
  ansible-lint .
scenario:
  test_sequence:
    # - 'dependency'
    # - 'cleanup'
    - 'destroy'
    - 'syntax'
    - 'create'
    # - 'prepare'
    - 'converge'
    - 'idempotence'
    # - 'side_effect'
    - 'verify'
    # - 'cleanup'
    - 'destroy'
