---
###############################################################################
# Converge
###############################################################################
#
# Generates:
#   _test_systemd_cache_default_stat: stat /etc/systemd/system

- name: 'Converge'
  hosts: 'all'
  gather_facts: false
  tasks:
    - name: 'Converge | snapsnot systemd directory'
      ansible.builtin.stat:
        path: '/etc/systemd/system'
      register: _test_systemd_default_stat

    - name: 'Converge | export snapshot to cache for verification'
      ansible.builtin.set_fact:
        _test_systemd_cache_default_stat: '{{ _test_systemd_default_stat }}'
        cacheable: true

    - name: 'Converge | apply r_pufky.deb.systemd'
      ansible.builtin.include_role:
        name: 'r_pufky.deb.systemd'
